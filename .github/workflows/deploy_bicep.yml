name: Deploy Bicep and Trigger TS Repo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy_bicep:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Login to Azure
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # 3️⃣ Check if the resource group has resources
      - name: List resources in resource group
        id: list_resources
        run: |
          RESOURCE_GROUP="rg-taskGen-dev-weu"
          RESOURCES=$(az resource list --resource-group $RESOURCE_GROUP --query "[].id" -o tsv | tr '\n' ',' | sed 's/,$//')
          echo "resources=$RESOURCES" >> $GITHUB_OUTPUT

      # 4️⃣ Deploy Bicep template
      - name: Deploy Bicep (complete mode)
        run: |
          RESOURCE_GROUP="rg-taskGen-dev-weu"
          LOCATION="westeurope"
          TEMPLATE_FILE="main.bicep"
          DEPLOYMENT_NAME="main-$(date +%s)"  # unique name using timestamp
      
          az group create --name $RESOURCE_GROUP --location $LOCATION
      
          az deployment group create \
            --name $DEPLOYMENT_NAME \
            --resource-group $RESOURCE_GROUP \
            --template-file $TEMPLATE_FILE \
            --parameters main.bicepparam \
            --parameters \
              jwtSecret="${{ secrets.JWT_SECRET }}" \
              ddimitrPass="${{ secrets.ddimitr_dummy_password }}" \
              helloPass="${{ secrets.hello_dummy_password }}"
              
      # 5️⃣ & 6️⃣ Capture App Service principal ID and deploy role assignments
      - name: Deploy Role Assignments (Key Vault Reader + Storage Table Contributor)
        run: |
          RESOURCE_GROUP="rg-taskGen-dev-weu"
          ROLES_DEPLOYMENT_NAME="roles-$(date +%s)"

          # Extract subscription ID from AZURE_CREDENTIALS JSON
          SUBSCRIPTION_ID=$(echo '${{ secrets.AZURE_CREDENTIALS }}' | jq -r '.subscriptionId')
          if [ -z "$SUBSCRIPTION_ID" ]; then
            echo "❌ Subscription ID not found in AZURE_CREDENTIALS."
            exit 1
          fi

          # Get App Service principal ID from main deployment output

          PRINCIPAL_ID=$(az webapp identity show \
            --name app-service-taskGen-dev \
            --resource-group rg-taskGen-dev-weu \
            --query principalId -o tsv)
          echo "PRINCIPAL_ID=$PRINCIPAL_ID" >> $GITHUB_ENV


          if [ -z "$APP_PRINCIPAL_ID" ]; then
            echo "❌ App Service principal ID is empty. Cannot assign roles."
            exit 1
          fi
          echo "App Service principal ID: $APP_PRINCIPAL_ID"

          # Build role assignments JSON dynamically
          ROLE_ASSIGNMENTS_JSON=$(jq -n --arg pid "$APP_PRINCIPAL_ID" --arg sub "$SUBSCRIPTION_ID" '{
            "roleAssignments": {
              "value": [
                {
                  "principalId": $pid,
                  "roleDefinitionId": "/subscriptions/\($sub)/resourceGroups/rg-taskGen-dev-weu/providers/Microsoft.Authorization/roleDefinitions/21090545-7ca7-4776-b22c-e363652d74d2"
                },
                {
                  "principalId": $pid,
                  "roleDefinitionId": "/subscriptions/\($sub)/resourceGroups/rg-taskGen-dev-weu/providers/Microsoft.Authorization/roleDefinitions/0a9a7e1f-b9d0-4cc4-a60d-0319b160aaa3"
                }
              ]
            }
          }')

          # Deploy roles using idempotent roles.bicep
          set +e
          az deployment group create \
            --name $ROLES_DEPLOYMENT_NAME \
            --resource-group $RESOURCE_GROUP \
            --template-file roles.bicep \
            --parameters "$ROLE_ASSIGNMENTS_JSON"
          EXIT_CODE=$?
          set -e

          # Handle "already exists" gracefully
          if [ $EXIT_CODE -ne 0 ]; then
            echo "🔍 Checking if role assignments already exist..."
            EXISTS=$(az role assignment list --assignee $APP_PRINCIPAL_ID --query "[?roleDefinitionId=='/subscriptions/$SUBSCRIPTION_ID/resourceGroups/rg-taskGen-dev-weu/providers/Microsoft.Authorization/roleDefinitions/21090545-7ca7-4776-b22c-e363652d74d2' || roleDefinitionId=='/subscriptions/$SUBSCRIPTION_ID/resourceGroups/rg-taskGen-dev-weu/providers/Microsoft.Authorization/roleDefinitions/0a9a7e1f-b9d0-4cc4-a60d-0319b160aaa3']" -o tsv)
            if [ -n "$EXISTS" ]; then
              echo "✅ Roles already exist — continuing."
            else
              echo "❌ Role assignment failed for a real reason."
              exit 1
            fi
          else
            echo "✅ Role assignments deployed successfully."
          fi

